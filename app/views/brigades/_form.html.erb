<%= form_for(@brigade) do |f| %>
  <% if @brigade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brigade.errors.count, "error") %> prohibited this brigade from being saved:</h2>

      <ul>
      <% @brigade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :workers %><br />
    <%= f.number_field :workers %>
  </div>
  <div class="field">
    <%= f.label :cost %><br />
    <%= f.number_field :cost %>
  </div>
  <div class="field">
    <%= f.label 'Skills' %><br />
    <%= f.text_field :tag_list %>
  </div>
  <div class="field">
    <%= f.label :country %><br />
    <%= f.select :country_id, countries_for_select %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
